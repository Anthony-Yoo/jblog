<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Admin">

	<insert id="updateBlog" parameterType="com.jblog.vo.BlogVo">
		<![CDATA[
			update blog 
			set 			
			blogTitle = #{blogTitle},
			logoFile = #{logoFile}	
			where id = #{id}				
		]]>
	</insert>
	
	<select id="selectId" parameterType="String" resultType="com.jblog.vo.CategoryVo">
		<![CDATA[
			select  c.cateNo,
					c.id,
					c.cateName,
					c.description,
					c.regDate,
					(select count(*) from post p where c.cateNo = p.cateNo) as postNum
			from category c
			where c.id = #{id}
			order by c.cateNo DESC		
		]]>
	</select>
	
	<insert id="insertcateVo" parameterType="com.jblog.vo.CategoryVo">
		<selectKey keyProperty="cateNo" resultType="int" order="BEFORE">
			select seq_category_no.nextval from dual		
		</selectKey>
		insert into 
		category(	cateNo,
					id,
					cateName,
					description,
					regDate	)
		values (	#{cateNo},
					#{id},
					#{cateName},
					#{description},
					sysdate	)	
	</insert>
	
	<select id="selectCno" parameterType="int" resultType="com.jblog.vo.CategoryVo">
		<![CDATA[
			select  c.cateNo,
					c.id,
					c.cateName,
					c.description,
					c.regDate,
					(select count(*) from post p where c.cateNo = p.cateNo) as postNum
			from category c
			where c.cateNo = #{cateNo}
		]]>
	</select>
	
	<delete id="deleteVo" parameterType="int">
		<![CDATA[
			delete from category
			where cateNo = #{cateNo}
		]]>	
	</delete>
	<insert id="insertpostVo" parameterType="com.jblog.vo.PostVo">		
		insert into 
		post  (		postNo,
					cateNo,
					postTitle,
					postContent,
					regDate	)
		values (	seq_post_no.nextval,
					#{cateNo},
					#{postTitle},
					#{postContent},
					sysdate	)	
	</insert>
	
	
	
</mapper>