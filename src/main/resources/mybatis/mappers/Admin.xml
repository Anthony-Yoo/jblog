<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Admin">

	<insert id="updateBlog" parameterType="com.jblog.vo.BlogVo">
		<![CDATA[
			update blog 
			set 			
			blogTitle = #{blogTitle},
			logoFile = #{logoFile}	
			where id = #{id}				
		]]>
	</insert>
	
	<select id="selectId" parameterType="String" resultType="com.jblog.vo.CategoryVo">
		<![CDATA[
			select  c.cateNo,
					c.id,
					c.cateName,
					c.description,
					to_char(c.regDate, 'yy/mm/dd') as regDate,
					(select count(*) from post p where c.cateNo = p.cateNo) as postNum
			from category c
			where c.id = #{id}
			order by c.cateNo DESC		
		]]>
	</select>
	
	<insert id="insertcateVo" parameterType="com.jblog.vo.CategoryVo">
		<selectKey keyProperty="cateNo" resultType="int" order="BEFORE">
			select seq_category_no.nextval from dual		
		</selectKey>
		insert into 
		category(	cateNo,
					id,
					cateName,
					description,
					regDate	)
		values (	#{cateNo},
					#{id},
					#{cateName},
					#{description},
					sysdate )	
	</insert>
	
	<select id="selectCno" parameterType="int" resultType="com.jblog.vo.CategoryVo">
		<![CDATA[
			select  c.cateNo,
					c.id,
					c.cateName,
					c.description,
					to_char(c.regDate, 'yy/mm/dd') as regDate,
					(select count(*) from post p where c.cateNo = p.cateNo) as postNum
			from category c
			where c.cateNo = #{cateNo}
		]]>
	</select>
	
	<delete id="deleteCateVo" parameterType="int">
		<![CDATA[
			delete from category
			where cateNo = #{cateNo}
		]]>	
	</delete>
	
	<insert id="insertPostVo" parameterType="com.jblog.vo.PostVo">
		<![CDATA[		
			insert into 
			post  (		postNo,
						cateNo,
						postTitle,
						postContent,
						regDate	)
			values (	seq_post_no.nextval,
						#{cateNo},
						#{postTitle},
						#{postContent},
						sysdate )	
		]]>
	</insert>
	
	<select id="selectPostList" resultType="com.jblog.vo.PostVo">
		<![CDATA[
			select  postNo,
					cateNo,
					postTitle,
					postContent,
					to_char(regDate, 'yy/mm/dd') as regDate
			from 	post
			Order by postNo DESC		
		]]>	
	</select>
	
	<select id="selectListPost" parameterType="int" resultType="com.jblog.vo.PostVo">
		<![CDATA[
			select  postNo,
					cateNo,
					postTitle,
					postContent,
					to_char(regDate, 'yy/mm/dd') as regDate
			from 	post
			where cateNo = #{cateNo}
			Order by postNo DESC		
		]]>	
	</select>
	
	<select id="selectPost" parameterType="int" resultType="com.jblog.vo.PostVo">
		<![CDATA[
			select  postNo,
					cateNo,
					postTitle,
					postContent,
					to_char(regDate, 'yy/mm/dd') as regDate
			from 	post
			where postNo = #{postNo}		
		]]>	
	</select>
	
	<insert id="insertCmteVo" parameterType="com.jblog.vo.CommentsVo">
		<selectKey keyProperty="cmtNo" resultType="int" order="BEFORE">
			select seq_comments_no.nextval from dual		
		</selectKey>
		insert into 
		comments(	cmtNo,
					postNo,
					userNo,
					cmtContent,
					regDate	)
		values (	#{cmtNo},
					#{postNo},
					#{userNo},
					#{cmtContent},
					sysdate )	
	</insert>
	
	<select id="selectCmt" parameterType="int" resultType="com.jblog.vo.CommentsVo">
		<![CDATA[
			select  c.cmtNo,
					c.postNo,
					c.userNo,
					c.cmtContent,
					to_char(c.regDate, 'yy/mm/dd') as regDate,
					(select userName from users u where c.userNo = u.userNo) as userName
			from comments c
			where c.cmtNo = #{cmtNo}
		]]>
	</select>
</mapper>